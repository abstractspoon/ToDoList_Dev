dnl Process this file with autoconf to produce a configure script.
AC_INIT()

dnl  default installation
dnl will be overridden by command-line args from packages/configure
AC_PREFIX_DEFAULT(../../../..)

dnl this uses the config.guess, config.sub and install-sh from gsdl.
AC_CONFIG_AUX_DIR(../../../..)

dnl compiler - only c, don't need c++
AC_PROG_CC

dnl Windows extension
AC_EXEEXT

AC_PROG_AWK
AC_PROG_RANLIB
AC_PROG_INSTALL

dnl Checks for header files.
dnl AC_HEADER_DIRENT
dnl AC_HEADER_STDC

AC_OUTPUT(  Makefile Libs/h/Makefile Libs/lib/Makefile Libs/lib-unix/Makefile Libs/OTHER/Makefile )
